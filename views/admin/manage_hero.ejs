<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Manage Hero Section</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">
    <div class="container mt-5">
        <a href="/admin/dashboard" class="btn btn-secondary mb-3">&larr; Back to Dashboard</a>
        <h2>Manage Hero Section</h2>

        <div class="card p-4">
            <form action="/admin/update_hero" method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label>Main Heading (Title)</label>
                    <input type="text" name="title" class="form-control" value="<%= hero.title %>" required>
                </div>
                <div class="mb-3">
                    <label>Subtitle (e.g. Roles/Designation)</label>
                    <textarea name="subtitle" class="form-control" rows="3"><%= hero.subtitle %></textarea>
                </div>
                <div class="mb-3">
                    <label>Current Image</label><br>
                    <img src="<%= hero.image_path %>" style="width: 150px; height: auto;" class="img-thumbnail mb-2">
                    <input type="file" name="image_path" class="form-control" accept="image/*">
                    <small class="text-muted">Upload to change image.</small>
                </div>
                <button type="submit" class="btn btn-primary">Update Hero Section</button>
            </form>
        </div>
    </div>
</body>

</html>