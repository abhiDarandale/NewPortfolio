<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Manage Site Settings</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">
    <div class="container mt-5">
        <a href="/admin/dashboard" class="btn btn-secondary mb-3">&larr; Back to Dashboard</a>
        <h2>Manage Site Settings</h2>

        <div class="card p-4">
            <form action="/admin/update_settings" method="POST">
                <div class="mb-3">
                    <label>Navbar Brand (Name/Logo Text)</label>
                    <input type="text" name="navbar_brand" class="form-control" value="<%= settings.navbar_brand %>"
                        required>
                </div>
                <div class="mb-3">
                    <label>Footer Text</label>
                    <textarea name="footer_text" class="form-control" rows="2"><%= settings.footer_text %></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Update Settings</button>
            </form>
        </div>
    </div>
</body>

</html>