<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Manage About Section</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">
    <div class="container mt-5">
        <a href="/admin/dashboard" class="btn btn-secondary mb-3">&larr; Back to Dashboard</a>
        <h2>Manage About Section</h2>

        <div class="card p-4">
            <form action="/admin/update_about" method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label>Paragraph 1 (Intro)</label>
                    <textarea name="description_1" class="form-control" rows="2"><%= about.description_1 %></textarea>
                </div>
                <div class="mb-3">
                    <label>Paragraph 2 (Education)</label>
                    <textarea name="description_2" class="form-control" rows="3"><%= about.description_2 %></textarea>
                </div>
                <div class="mb-3">
                    <label>Paragraph 3 (Current Role)</label>
                    <textarea name="description_3" class="form-control" rows="3"><%= about.description_3 %></textarea>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label>Profile Image</label><br>
                        <img src="<%= about.image_path %>" style="width: 100px; height: auto;"
                            class="img-thumbnail mb-2">
                        <input type="file" name="image_path" class="form-control" accept="image/*">
                    </div>
                    <div class="col-md-6 mb-3">
                        <label>CV / Resume (PDF)</label><br>
                        <p>Current CV: <a href="<%= about.cv_path %>" target="_blank">Download Code</a></p>
                        <input type="file" name="cv_path" class="form-control" accept=".pdf">
                    </div>
                </div>

                <button type="submit" class="btn btn-primary">Update About Section</button>
            </form>
        </div>
    </div>
</body>

</html>